version: "3.8"

services:
  web:
    build: .
    container_name: quoteapp-container
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run start
