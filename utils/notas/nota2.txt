Cosas por terminar

-- El desplazmiento de la tabla result a la linea modificada (check)

-- El cuadro para introducir varios manufacturer part en un solo input y hacer el calculo

//cod para ver el height del tr en divTableResultScroll
document.addEventListener("click",(e)=>{
    //console.log(document.getElementById("divTableResultScroll"));
    if(e.target.nodeName === "STRONG"){
        let tr = e.target.closest("tr");
        console.log("cor click: ", tr.offsetTop);
    }
})